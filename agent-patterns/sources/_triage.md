# _triage

One row per item seen. This is the only file the orchestrator reads to choose
what to distill. `note-exists?` = a `sources/<id>.md` note exists.

| id | date | title/author | relevance | scope | note-exists? | one-line takeaway |
|-|-|-|-|-|-|-|
