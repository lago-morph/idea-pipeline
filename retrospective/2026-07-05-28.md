# Retrospective — task-02: authoring the idea→spec process specification (process.md)

- **UTC date**: 2026-07-05 (verified via `date -u +%Y-%m-%d`, cross-checked with `python3 datetime.UTC` — both agree)
- **Last PR**: #28 (highest PR number covered by this retro)
- **Branch at write time**: claude/idea-to-spec-process-ztl18z (restarted from the merged main)
- **Sibling artifacts**: [./2026-07-05-28/](./2026-07-05-28/)
- **Scope**: full session (from branch divergence; the branch was reset onto the merged main immediately before this retro, so the covered work is exactly PR #28)

## Commit hashes by PR

### PR #28 — claude/idea-to-spec-process-ztl18z (merged 2026-07-05)

- `7dcc923` Add idea→spec process spec (task-02, #22)
- `a8f9ac1` Merge pull request #28 (merge commit)

## Part 1 — what happened

### Phase 1 — Context load per the task file's reading list

The session executed `spec-completeness/tasks/task-02-idea-to-spec-process.md` as a prompt. Its "Context to load" list was read in the stated order: README §3 and §4.2; the merged `artifact-model.md` (979 lines — over the Read cap, consumed in two offset slices); one profile ambiguity audit (attractor, dimension 13); `tasks/README.md` and `task-03` (whose hardened checks slot into the process's stage gates); and four numbered idea files to calibrate entry expectations (73–113-line briefs, each with its own Open Questions section). No deviations — the tasks' self-contained-prompt convention held exactly as designed.

### Phase 2 — Design before drafting

The load-bearing design work happened before any prose: mapping all sixteen artifact-model artifacts onto exactly one producing stage each (S1: A-VS; S2: A-GL; S3: C-DM/C-BC/C-FM/C-QC/R-FR; S4: R-AS/R-IS/R-RA/R-CD/X-WE; S5: G-AC/G-CM/G-ST; S6: X-DR). Three tensions the task sketch left open were resolved with stated reasons, and all three became ADR drafts (Part 4): R-FR ownership in the contract stage, probe slices including the settlement ledger, and freeze-by-replication. Gate constants were chosen and justified in a single table (D_dry=2, M_abort=3, I_S3max=8, k_iter=3, k_freeze=5, R_freeze=2, R_S7max=6) so the pressure test can tune stated numbers.

### Phase 3 — Authoring in three sentinel chunks

The ~1,500-line document was written as one Write ending in a `<!--CONT-->` sentinel plus two Edits replacing the sentinel with the next chunk — boundaries at top-level section breaks (after §4; after §6). Zero truncation, zero tail re-reads. The README cross-reference paragraph was added in the same pass. This mechanic is now [`SKILL-SPEC-e8b8feb64f-sentinel-chunked-doc-write`](./2026-07-05-28/SKILL-SPEC-e8b8feb64f-sentinel-chunked-doc-write.md).

### Phase 4 — Re-read passes (12 defects, all pre-merge)

The post-edit-reread-pass skill ran to its stopping rule in three passes. Pass 1 (checker script + full read) caught 7 defects: two references to a nonexistent "§0"; principle P3 pointing at the S4 card for rationale living in the S3 card; the P-ASSUME charter contradicting S1's re-probe activity; X-WE carrying no exit criterion (added S4-X6); the S6 hygiene rule leaving status APPLIED neither open nor terminal; A-GL's growth violating the change-control rule (exception declared); and mermaid fragility (`&` in a node label, a hyphenated state name). Pass 2 (fresh full read) caught 5 more: abort-mechanics prose contradicting the stall counter; major-thaw missing R-FR narrowing; S0-X1's "all fields" clashing with an optional field; S4-E2 contradicting the A-GL exception; one awkward phrase. Pass 3 was clean: a vocabulary grep sweep (7 constants, 40 criterion labels, 24 metric names — all resolving) plus both mermaid diagrams validated. One Edit failed mid-pass because old_string was typed from memory for text written ~40 calls earlier; the PR-26 retro's grep-before-re-edit rule fired in anger and recovered it in one grep.

### Phase 5 — Ship and watch

Single commit with per-pass findings enumerated in the body; push; PR #28 opened ready-for-review with the task's acceptance checklist ticked. The session auto-subscribed to PR activity; the repo has no CI (zero check runs), so the watch reduced to reviews + merge; a 60-minute self check-in was armed via send_later.

### Phase 6 — Merge and teardown

On "merge and retrospect": merged #28 by merge commit (repo convention), unsubscribed from PR activity, and deleted the check-in trigger — all in the same turn — then restarted the designated branch from the merged main for this retrospective, exactly per the PR-26 retro's merge-mechanics rule.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 — single-context authoring session |
| PRs opened / merged | 1 / 1 (#28) |
| Real-world bugs discovered + fixed | 12 doc defects pre-merge (passes 1–2: 7 + 5); 0 found after merge |
| Tests added (before / after) | 0 / 0 — verification was 3 re-read passes, a vocabulary grep sweep, and 2 diagram validations |
| Spec amendments | 2 stated deviations from the task-02 design sketch (probe slice includes R-FR; artifact coverage extended from the sketch's 12 IDs to the model's 16) |
| Scenarios driven / skipped | n/a (documentation session) |
| Files touched at major refactors | 2 files, +1,508 lines |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `sentinel-chunked-doc-write` | SKILL-SPEC-e8b8feb64f | high | authoring mechanics | [./2026-07-05-28/SKILL-SPEC-e8b8feb64f-sentinel-chunked-doc-write.md](./2026-07-05-28/SKILL-SPEC-e8b8feb64f-sentinel-chunked-doc-write.md) |
| `doc-vocabulary-sweep` | SKILL-SPEC-b619808de0 | high | doc verification | [./2026-07-05-28/SKILL-SPEC-b619808de0-doc-vocabulary-sweep.md](./2026-07-05-28/SKILL-SPEC-b619808de0-doc-vocabulary-sweep.md) |

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule
has its own per-rule file in the sibling directory at
`./2026-07-05-28/AGENTS-MD-<hash>-<kebab-name>.md` — the per-rule file contains
exactly two sections: `# agent instruction` (the verbatim rule text the
CI/CD assembler concatenates into `AGENTS.md`) and `# justification`
(the persuasion text the assembler strips out).

This Part 3 mirrors that content inline so the retrospective reviewer
has every proposed rule + its justification in one place at
review-decision time.

Dedupe note: two further candidates from this session — grep-before-re-Edit
on aged text, and jq-ing oversized saved MCP results — were dropped as
duplicates of rules already filed by the PR-26 retrospective
(`AGENTS-MD-e8a2ee6701`, `AGENTS-MD-f79b564822`); both fired in anger this
session, which is re-confirmation, not a new proposal.

### Suggestion 1: Name it before you cite it — `AGENTS-MD-d41772f17d`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-d41772f17d-name-it-before-you-cite-it.md](./2026-07-05-28/AGENTS-MD-d41772f17d-name-it-before-you-cite-it.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Name it before you cite it.** Before writing a cross-reference in any document, confirm the target has a stable citable identity — a numbered section, a labeled criterion, a named table. If the target is an unnumbered structure (an intro, a header table), refer to it in words ("the header table at the top of this document"); never invent shorthand like "§0" for something that has no number.
>
> *Grounded in: two "§0" references to process.md's unnumbered intro, caught only in the re-read pass.*

**Justification** (verbatim from the per-rule file's `# justification` section):

While drafting process.md's appendix, the intro's four-question table needed citing twice; because the intro had no section number, "§0" got invented on the spot — twice, in two different passes — and both references shipped into the draft. They were caught only by the re-read pass's section-reference grep; had they survived, the pressure-test executor (whose defect records cite section labels) would have hit a dangling reference at the exact moment the protocol tells them to be precise. The rule costs one second of thought per cross-reference; a phantom reference costs a reader a search of the whole document for a section that does not exist.

### Suggestion 2: One table per name class — `AGENTS-MD-13cdbfea26`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-13cdbfea26-one-table-per-name-class.md](./2026-07-05-28/AGENTS-MD-13cdbfea26-one-table-per-name-class.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **One table per name class.** When a document defines named things used across sections — constants, metric names, criterion labels, statuses — define each name class in exactly one table and use the names verbatim everywhere else. Consistency then becomes a grep sweep (every use resolves to one definition; every definition is used) instead of a judgment call.
>
> *Grounded in: process.md's §9.1 constants and §9.2 metrics tables, which let one grep pass verify 7 constants, 40 criterion labels, and 24 metric names.*

**Justification** (verbatim from the per-rule file's `# justification` section):

process.md carries three name classes (7 gate constants, 24 metric names, 40 entry/exit criterion labels) used across eight stage cards, a routing table, and a pressure-test appendix. Because each class was defined in exactly one table and used verbatim, the final consistency check was a 20-second grep loop that mechanically proved every name resolves — across ~1,500 lines. The counter-case is visible in the gold-spec audits this repo studies: symphony's "configured assignee" — a name used in prose that resolves to no definition anywhere — survived review and became a canonical defect example. Tabling names first costs nothing at authoring time; it converts an unbounded proofreading problem into a bounded mechanical one.

### Suggestion 3: Discipline docs must pass their own discipline — `AGENTS-MD-3dd80c6d14`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-3dd80c6d14-discipline-docs-pass-own-discipline.md](./2026-07-05-28/AGENTS-MD-3dd80c6d14-discipline-docs-pass-own-discipline.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Discipline docs must pass their own discipline.** Before shipping a document that prescribes rules (complete enums, mechanical exit criteria, change control), audit the document against its own prescriptions: every enum it defines is complete, every criterion it states is evaluable over its own schemas, and every general rule it declares either holds internally or carries a declared exception. The document's own prescriptions are its review checklist.
>
> *Grounded in: three process.md defects found by self-application — an "all fields filled" criterion contradicting an optional field, a status (APPLIED) that was neither open nor terminal under the hygiene rule, and a change-control rule its own A-GL artifact could not obey.*

**Justification** (verbatim from the per-rule file's `# justification` section):

Three of the twelve defects caught in process.md's re-read passes were self-application failures: exit criterion S0-X1 demanded "all fields filled" over a schema with an optional field; the S6 question-hygiene rule enumerated the open statuses but left APPLIED (neither open nor terminal) in limbo; and the change-control rule "artifacts go CONTROLLED at producing-stage exit" was violated by the document's own A-GL design. None of these is visible to a reader checking prose flow — each is visible instantly to a reader asking "does this document obey the rule it just stated?". The audit costs one targeted pass; shipping a process spec that contradicts itself costs credibility exactly where the document demands rigor from others, and every such contradiction becomes a process-defect record the first time someone executes it.

### Suggestion 4: Declare the exception at the rule — `AGENTS-MD-85093fcdff`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-85093fcdff-declare-the-exception-at-the-rule.md](./2026-07-05-28/AGENTS-MD-85093fcdff-declare-the-exception-at-the-rule.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Declare the exception at the rule.** When a general rule meets a case that genuinely cannot comply, write the exception into the rule's definition site and into every place the rule binds — never leave it implicit for readers to reconcile. An undeclared exception is a latent contradiction; a declared one is a design decision.
>
> *Grounded in: A-GL's glossary half violating "artifacts go CONTROLLED at producing-stage exit" until the exception was declared at the rule (§3.3), the producing stage card (S2), and the production map (§7).*

**Justification** (verbatim from the per-rule file's `# justification` section):

process.md's change-control rule is one sentence; the A-GL glossary genuinely cannot obey it (terms accrue with every artifact that introduces them, long after A-GL's producing stage exits). The first draft left that tension implicit, and the re-read flagged it as a contradiction a pressure-test executor would have had to improvise around — the exact failure mode (SCHEMA_MISFIT, ruling required) the pressure test exists to count. The fix cost three sentences, one at each site where the rule binds. The asymmetry: declaring an exception is a few lines at authoring time; an undeclared one becomes a judgment call made independently — and differently — by every future executor, which is the definition of the ambiguity this whole repo is trying to eliminate.

### Suggestion 5: Dedupe retro proposals against prior retros — `AGENTS-MD-e99f819f8b`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-e99f819f8b-dedupe-retro-proposals.md](./2026-07-05-28/AGENTS-MD-e99f819f8b-dedupe-retro-proposals.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Dedupe retro proposals against prior retros.** Before proposing agents-file rules or skills in a retrospective, list what earlier retros already proposed (`ls retrospective/*/AGENTS-MD-*.md retrospective/*/SKILL-SPEC-*.md`) and read the rule names; drop any candidate an existing rule already covers, and cite the prior rule instead if the session re-confirmed it.
>
> *Grounded in: this session nearly re-proposing grep-before-re-edit and jq-the-saved-result, both already filed by the PR-26 retrospective.*

**Justification** (verbatim from the per-rule file's `# justification` section):

Two of this session's strongest workaround candidates — grep the file before re-Editing aged text, and jq the saved result file for oversized MCP outputs — turned out to already exist verbatim in the PR-26 retrospective's sibling directory, discovered only because a pre-write `ls` of prior AGENTS-MD files happened as a formatting check. Re-proposing them would have produced two near-identical rules with different hashes for the downstream assembler to reconcile, and diluted the review signal of the genuinely new rules. The dedupe costs one ls plus a minute of reading names; the alternative grows the rule set superlinearly with session count while adding nothing. Re-confirmation is still signal — note it in the narrative ("the rule fired in anger"), not as a duplicate proposal.

### Suggestion 6: Tear down PR-watch machinery on merge — `AGENTS-MD-4e5981faa2`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-4e5981faa2-teardown-pr-watch-on-merge.md](./2026-07-05-28/AGENTS-MD-4e5981faa2-teardown-pr-watch-on-merge.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Tear down PR-watch machinery on merge.** The moment a watched PR merges or closes, in the same turn: unsubscribe from its activity and delete every send_later / self check-in trigger armed for it (use list_triggers if the trigger ID has scrolled out of context). An orphaned trigger fires into a finished task, wastes a turn re-deriving that nothing is left to do, and can ping the user about a PR that no longer exists.
>
> *Grounded in: merging PR #28 and deleting its 60-minute check-in trigger alongside the unsubscribe, in the same turn.*

**Justification** (verbatim from the per-rule file's `# justification` section):

This session armed a 60-minute self check-in for PR #28 per the watch protocol, then merged the PR eleven minutes later on the user's instruction. Had the trigger survived the merge, it would have fired into a session that had already moved on to retrospective work, forcing a re-derivation of PR state mid-task purely to conclude "merged, nothing to do" — and the re-arm instruction embedded in the trigger prompt risks the loop persisting indefinitely. The teardown is two tool calls and only cheap while the trigger ID is still in context; afterwards it requires a list_triggers hunt. Watch machinery is easy to arm and invisible once armed — symmetry (arm on subscribe, tear down on merge) is the only reliable discipline.

### Suggestion 7: Write mermaid labels defensively and validate before commit — `AGENTS-MD-3a908661b1`

- **Per-rule file**: [./2026-07-05-28/AGENTS-MD-3a908661b1-defensive-mermaid-labels.md](./2026-07-05-28/AGENTS-MD-3a908661b1-defensive-mermaid-labels.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Write mermaid labels defensively and validate before commit.** In mermaid sources: no bare `&` in node labels (write "and"), no hyphens in state names (alias them: `state "PENDING-OWNER" as PO`), and validate every diagram before committing (Mermaid MCP validator; extract `.valid` from the saved result). A diagram that fails to render ships as a code block of noise — worse than no diagram.
>
> *Grounded in: pre-emptive `&`-to-"and" and quoted-alias fixes in process.md's two diagrams, both validated true before commit.*

**Justification** (verbatim from the per-rule file's `# justification` section):

process.md's stage graph originally read `S6[S6 Assembly & lint]` and its question lifecycle needed a state literally named PENDING-OWNER — both constructs that mermaid variants mis-parse (`&` as an entity boundary, `-` as illegal in state identifiers). Caught in the re-read pass and fixed defensively ("and"; `state "PENDING-OWNER" as PO`), then both diagrams were validated true via the Mermaid MCP tool before commit. The failure mode is nasty precisely because it is invisible locally: the markdown looks fine, the PR diff looks fine, and only the rendered GitHub page shows a grey error block where the process's central diagram should be — in the merged, linked-from-README deliverable. Validation costs two tool calls per diagram; un-rendering costs a follow-up PR and a reader who bounced off the document's most load-bearing visual.


## Part 4 — proposed ADRs

Architectural decisions made in this session that the user may want to
record as ADRs. Each candidate has a **full draft** in the sibling
directory, carrying a frozen `ADR-<hash>` ID. The user can adopt any
draft by invoking the `adr` skill, which moves it to
`docs/adr/NNNN-kebab-title.md` while preserving the ID.

| ID | Title | Draft |
|----|-------|-------|
| ADR-e39a6ae679 | The Freedom Register is produced by the contract stage | [./2026-07-05-28/ADR-e39a6ae679-freedom-register-produced-by-contract-stage.md](./2026-07-05-28/ADR-e39a6ae679-freedom-register-produced-by-contract-stage.md) |
| ADR-10bcebf036 | Probe context slices include the settlement ledger | [./2026-07-05-28/ADR-10bcebf036-probe-slices-include-settlement-ledger.md](./2026-07-05-28/ADR-10bcebf036-probe-slices-include-settlement-ledger.md) |
| ADR-b1b74062d9 | Freeze requires a replication round on byte-identical text | [./2026-07-05-28/ADR-b1b74062d9-freeze-replication-byte-identical.md](./2026-07-05-28/ADR-b1b74062d9-freeze-replication-byte-identical.md) |

One-line rationale per draft appears in each draft's `## Context`
opening sentence; no need to duplicate it here.
