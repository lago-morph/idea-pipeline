# Retrospective — agent-patterns wiki MVP built from spec through Checkpoint B (Phases 0–4)

- **UTC date**: 2026-09-09 (verified via `date -u +%Y-%m-%d`, confirmed by `python3 datetime.UTC`)
- **Last PR**: #39 (highest PR number covered by this retro)
- **Branch at write time**: claude/agent-patterns-spec-th583c (restarted from origin/main after PR #39 merged)
- **Sibling artifacts**: [./2026-09-09-39/](./2026-09-09-39/)
- **Scope**: full session (all five commits of PR #39; branch was merged before this retro was written, so the git-driven scope heuristic doesn't apply — scope fixed to the PR's commit range)

## Commit hashes by PR

### PR #39 — claude/agent-patterns-spec-th583c (merged 2026-08-29)

- `86a2d8e` agent-patterns Phase 0: scaffold wiki structure per SPEC
- `e0607b9` agent-patterns Phase 1: acquire sources (log only; raw material gitignored)
- `622c2fb` agent-patterns Phase 2: triage all 234 cached sources
- `e4f0919` agent-patterns Phase 3: distill 49 source notes
- `aa1adbe` agent-patterns Phase 4: synthesize 30 patterns + 6 anti-patterns, quickref, index, skill

## Part 1 — what happened

The session executed `agent-patterns/SPEC.md` — a self-contained build plan for an agent-maintained wiki of practitioner patterns for single-human/single-agent coding sessions — from its §12 kickoff prompt through its Checkpoint B stopping point, in one pass. Jonathan later merged PR #39 without change requests, which served as the Checkpoint B OK.

### Phase 0 — scaffold

The spec resolved most of its own §11 open items via the environment: `every.mbox` (148 Every newsletters) was already committed to the repo, answering the Every-access question, and the default branch was confirmed as `main`. Two deviations were made and logged immediately: the session-mandated branch name replaced the spec's `feature/agent-patterns-mvp`, and `agent-patterns-SPEC.md` was renamed to the spec's own canonical `SPEC.md`. The notable mishap: after `git mv`-ing the mbox into gitignored `.cache/`, the file stayed staged as a rename — gitignore does not untrack indexed files — and needed an explicit `git rm --cached` to keep the newsletter corpus out of history, as SPEC §2 requires.

### Phase 1 — acquire

All 27 SPEC §10 URLs verified live (including `anthropic-2025-ccbp` at its original URL, which the spec suspected had moved). 86 web items were fetched into `.cache/web/` with a ~40-line urllib+html2text script written after discovering the sandbox ships no HTML-to-text library. Three "locate at kickoff" items were found (Codex docs, HumanLayer ACE, Ng's Batch piece). Osmani's RSS feed returned only 10 posts; the sitemap's `<lastmod>` data recovered the full history, yielding 40 AI-development posts since 2025-03-01. The mbox was split into per-message text files with Python's `mailbox` module.

### Phase 2 — triage

All 234 cached items (148 emails + 86 web files) were triaged by 16 Sonnet subagent batches of ~15 items each — a logged 80/20 deviation from the spec's one-item-per-invocation subagent design. Because 16 concurrent agents could not safely append to `sources/_triage.md`, each wrote a private fragment under `.cache/triage/`, merged afterward with a shell loop and validated by row/field counts (236 lines = 234 rows + header). Distribution: ~50 high, ~60 medium, ~70 low, ~55 marketing.

### Phase 3 — distill

Highs alone exceeded the spec's "~15–30 items" estimate, so multi-page works were consolidated under their SPEC §10 umbrella ids (18 Willison chapter files → one `willison-2026-aep` note; 5 Claude Code doc pages → `claude-code-docs`; 3 Codex pages → `codex-docs`) — now ADR-3ba016a5fe. Eleven Opus subagents wrote 49 source notes; each returned a ≤ 12-line digest of candidate pattern slugs, and every digest was appended to a scratchpad ledger (`digests.md`) on arrival as context-truncation insurance. All distiller prompts carried a shared seed-slug vocabulary; independent batches converged on identical coinages (`give-a-runnable-check` three times).

### Phase 4 — synthesize

The orchestrator clustered ~90 candidate slugs into 30 patterns + 6 anti-patterns itself, encoding every page's full metadata into one plan file (`pattern-plan.md`). Five Opus writer batches produced the 36 pages under an evidence-discipline contract — read each cited note, claim only what it claims, drop or narrow unsupported citations and report it. Writers reported real narrowings (e.g. `openhands-2026-ccbp` scoped to fresh-model *plan* review on `cross-model-review`). A lint pass found zero broken links or schema violations. Status split 31 adopted / 5 candidate under a two-distinct-sources promotion threshold (now ADR-ce4eb52bb1); `quickref.md` (46 lines), `index.md`, `bibliography.md` (62 rows), and the skill folder were generated. One extraction bug surfaced: the patterns-list regex missed the `**Use when (at risk of it):**` anti-pattern heading variant, leaving an empty field caught only by reading the generated file back.

### Post-build

The PR was babysat via an hourly `send_later` check-in; one check-in fired, found the PR clean, and re-armed. Jonathan merged ~100 minutes after Checkpoint B. Phase 5 (dogfood) remains blocked on the three task descriptions from SPEC §11 item 4.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 32 (16 Sonnet triage, 11 Opus distill, 5 Opus page-writers) |
| PRs opened / merged | 1 / 1 |
| Real-world bugs discovered + fixed | 3 (mbox tracked despite gitignore; mbox committed against spec policy; empty extracted field in generated patterns-list) |
| Tests added (before / after) | 0 / 0 (documentation corpus; lint script served as verification) |
| Spec amendments | 0 (two logged deviations: branch name, spec filename) |
| Scenarios driven / skipped | 4 phases driven / Phase 5 blocked on user input |
| Files touched at major refactors | 103 (PR #39: 49 source notes, 36 pattern pages, generated artifacts, scaffold) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `fragment-fanout-classify` | SKILL-SPEC-02be69a556 | high | subagent orchestration | [./2026-09-09-39/SKILL-SPEC-02be69a556-fragment-fanout-classify.md](./2026-09-09-39/SKILL-SPEC-02be69a556-fragment-fanout-classify.md) |
| `web-corpus-to-cache` | SKILL-SPEC-fd4e7ecbd1 | med | research ingest | [./2026-09-09-39/SKILL-SPEC-fd4e7ecbd1-web-corpus-to-cache.md](./2026-09-09-39/SKILL-SPEC-fd4e7ecbd1-web-corpus-to-cache.md) |
| `planned-evidence-synthesis` | SKILL-SPEC-6c1c282b73 | high | corpus synthesis | [./2026-09-09-39/SKILL-SPEC-6c1c282b73-planned-evidence-synthesis.md](./2026-09-09-39/SKILL-SPEC-6c1c282b73-planned-evidence-synthesis.md) |

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule has its own per-rule file in the sibling directory at `./2026-09-09-39/AGENTS-MD-<hash>-<kebab-name>.md` — the per-rule file contains exactly two sections: `# agent instruction` (the verbatim rule text the CI/CD assembler concatenates into `AGENTS.md`) and `# justification` (the persuasion text the assembler strips out). This Part 3 mirrors that content inline so the retrospective reviewer has every proposed rule + its justification in one place at review-decision time.

### Suggestion 1: Untrack files moved into a gitignored directory — `AGENTS-MD-fe49c408f8`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-fe49c408f8-untrack-files-moved-into-a-gitignored-directory.md](./2026-09-09-39/AGENTS-MD-fe49c408f8-untrack-files-moved-into-a-gitignored-directory.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Untrack files moved into a gitignored directory.** "After moving a tracked file into a gitignored path (via `git mv` or `mv`), run `git rm --cached <path>` and confirm with `git status` that the index shows a deletion, not a rename — `.gitignore` never untracks a file that is already in the index."
>
> *Grounded in: `every.mbox` staying staged as a rename into gitignored `.cache/` during the agent-patterns Phase 0 scaffold.*

**Justification** (verbatim from the per-rule file's `# justification` section):

During Phase 0 of the agent-patterns build (PR #39), SPEC §2 required moving the 11 MB `every.mbox` out of git into gitignored `.cache/`. The `git mv` succeeded — and `git status` quietly showed `R  agent-patterns/every.mbox -> agent-patterns/.cache/every.mbox`: the file was still fully tracked, headed into the next commit, because gitignore only filters *untracked* files. Had that commit landed, the repo would have carried the newsletter corpus the spec explicitly forbids committing, in a hard-to-notice location, requiring a history rewrite to purge. The fix was one command (`git rm --cached`) plus one glance at `git status`; the failure it prevents is a policy violation baked into history. The asymmetry is a few seconds against a permanent, license-relevant mistake.

### Suggestion 2: Give parallel writers private fragment files — `AGENTS-MD-d8904f210b`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-d8904f210b-give-parallel-writers-private-fragment-files.md](./2026-09-09-39/AGENTS-MD-d8904f210b-give-parallel-writers-private-fragment-files.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Give parallel writers private fragment files.** "When several concurrent subagents must each contribute rows or entries to one shared file, have each write its own fragment file (for example `.cache/<job>/<batch>.md`) and merge the fragments in the orchestrator afterward. Never instruct two concurrent agents to append to the same file."
>
> *Grounded in: 16 parallel triage batches merged conflict-free into `sources/_triage.md` in the agent-patterns build.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns SPEC designed its triage subagent to append rows directly to `sources/_triage.md` — fine for one-at-a-time ingest, but the Phase 2 build ran 16 triage subagents concurrently. Concurrent appends to one file interleave and clobber: rows land mid-row, or one agent's read-modify-write erases another's rows, and the corruption is silent until a later phase reads garbage. The build instead gave each batch a private fragment under `.cache/triage/` and merged with a three-line shell loop; 234 rows landed intact on the first try. The marginal cost is one merge step the orchestrator was going to do anyway; the avoided cost is re-running an entire fan-out (dozens of subagent invocations) to recover rows nobody can prove are missing.

### Suggestion 3: Batch small items ten to fifteen per subagent — `AGENTS-MD-39752b7a4d`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-39752b7a4d-batch-small-items-ten-to-fifteen-per-subagent.md](./2026-09-09-39/AGENTS-MD-39752b7a4d-batch-small-items-ten-to-fifteen-per-subagent.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Batch small items ten to fifteen per subagent.** "When fanning classification or triage work out over a corpus of many small items, give each subagent a batch of 10–15 items rather than one item per invocation — per-item dispatch spends more tokens on prompt overhead than on the work itself. Keep the per-item output contract identical to the single-item design."
>
> *Grounded in: 234 cached sources triaged in 16 batches instead of 234 invocations.*

**Justification** (verbatim from the per-rule file's `# justification` section):

Phase 2 of the agent-patterns build had 234 items to rate (148 newsletter emails plus 86 web fetches). The spec's one-item-per-invocation design would have meant 234 subagent dispatches, each paying the full prompt, role-file read, and return-envelope overhead to skim a file that is often two paragraphs of marketing. Batching 15 items per agent cut that to 16 dispatches with identical per-item output (one triage row each), and the whole phase finished in a few minutes of wall-clock. The rule's cost is nothing — the batch prompt is the single-item prompt with a file list. Its absence costs an order of magnitude in dispatch overhead, plus an orchestrator context flooded by 234 completion notifications.

### Suggestion 4: Persist subagent results to a scratchpad ledger as they arrive — `AGENTS-MD-2caf77725d`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-2caf77725d-persist-subagent-results-to-a-scratchpad-ledger-as-they-arrive.md](./2026-09-09-39/AGENTS-MD-2caf77725d-persist-subagent-results-to-a-scratchpad-ledger-as-they-arrive.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Persist subagent results to a scratchpad ledger as they arrive.** "When collecting results from many background subagents, append each returned digest to a single scratchpad ledger file the moment its completion notification arrives. Later phases then rebuild from disk instead of relying on conversation context that may be summarized away."
>
> *Grounded in: the `digests.md` ledger accumulating 11 distiller digests during the agent-patterns build.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns Phase 3 fan-out returned 11 distiller digests — roughly 90 candidate pattern slugs with one-line evidence each — that Phase 4's clustering depended on completely. Those digests arrived as conversation messages, exactly the material a long session's context summarization compresses first, and the build still had two heavy phases to go. Appending each digest to `scratchpad/digests.md` on arrival cost one small Bash call per notification; it turned the synthesis input from "whatever survives in context" into a file that could be re-read verbatim at any point. Without the ledger, losing those digests would have meant re-reading 49 source notes to reconstruct the slug inventory — the single most expensive artifact of the session to recompute.

### Suggestion 5: Seed a shared vocabulary before parallel naming — `AGENTS-MD-7000acb500`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-7000acb500-seed-a-shared-vocabulary-before-parallel-naming.md](./2026-09-09-39/AGENTS-MD-7000acb500-seed-a-shared-vocabulary-before-parallel-naming.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Seed a shared vocabulary before parallel naming.** "When multiple parallel subagents will coin identifiers (slugs, ids, filenames) that must later be merged, give every prompt the same seed list of preferred identifiers plus the convention for coining new ones. Independent agents then converge instead of inventing synonyms that need manual reconciliation."
>
> *Grounded in: three page-writer batches independently converging on `give-a-runnable-check` from shared seed-slug lists.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns build's distill and writer fan-outs all coined pattern slugs that had to merge into one flat namespace (SPEC D3 makes slugs permanent ids). Every distiller prompt carried the same seed-slug list plus the coining convention (lowercase-hyphenated, imperative). The payoff was visible: separate batches independently produced `give-a-runnable-check`, `specific-prompt-context`, and `course-correct-early` for the same underlying ideas — the W3 writer even reported the convergence unprompted. Without the seed list, ~90 candidate slugs from 16 independent agents would have arrived as ~90 near-synonyms (`runnable-check` vs `self-verification-tool` vs `verify-tool`), and deduplicating them is exactly the judgment-heavy merge work fan-outs are supposed to save. Cost: one reused paragraph per prompt.

### Suggestion 6: Drive parallel writers from one authoritative plan file — `AGENTS-MD-e0c12d3e97`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-e0c12d3e97-drive-parallel-writers-from-one-authoritative-plan-file.md](./2026-09-09-39/AGENTS-MD-e0c12d3e97-drive-parallel-writers-from-one-authoritative-plan-file.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Drive parallel writers from one authoritative plan file.** "When several subagents write files that must be mutually consistent — shared metadata fields, cross-references between files — put every per-file decision into one plan file they all read, and keep each agent's prompt to pointing at its section of the plan. Do not restate metadata inside each prompt: restated copies drift."
>
> *Grounded in: `pattern-plan.md` driving five writer batches to 36 pages with zero metadata inconsistencies at lint.*

**Justification** (verbatim from the per-rule file's `# justification` section):

Phase 4 of the agent-patterns build needed 36 pattern pages from five parallel writers, where every page's `related:` links had to name slugs another writer was creating and every frontmatter field had to survive a strict lint. All of it — slug, title, type, category, status, durability, confidence, sources, related — went into one `pattern-plan.md`, and each writer prompt said only "write the pages in your batch section". The post-write lint found zero problems: no broken related-links across writers, no status or category drift, no id/filename mismatches. The failure mode this prevents was demonstrated in miniature by the same session's SPEC, whose prose and §10 table disagreed about its own filename. Writing the plan file costs the planning you were doing anyway; per-prompt restatement costs a reconciliation pass over every inconsistency two copies can develop.

### Suggestion 7: Read a source before citing it — `AGENTS-MD-1b2f69d384`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-1b2f69d384-read-a-source-before-citing-it.md](./2026-09-09-39/AGENTS-MD-1b2f69d384-read-a-source-before-citing-it.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Read a source before citing it.** "An evidence or citation line may be written only after reading the cited source in this session, and may claim only what that source actually supports. When delegating writing, instruct subagents to drop or narrow unsupported citations from both the text and any metadata list, and to report every drop or narrowing back."
>
> *Grounded in: page writers scoping the `openhands-2026-ccbp` citation on `cross-model-review` down to fresh-model plan review.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns wiki's value rests on its no-hallucinated-evidence rule (SPEC §2), and the Phase 4 writers were the point of maximum risk: 36 pages, ~140 evidence lines, written from a plan that merely asserted which sources supported which patterns. Because every writer prompt required reading the cited note first and licensed dropping or narrowing, the discipline caught real errors: one writer scoped the OpenHands citation on `cross-model-review` to fresh-model *plan* review (the note doesn't support different-model *code* review), another confined an abstract-only arXiv note to what an abstract can claim, and a third recorded that `osmani-2026-code-review-ai` supports the threat-model tier rather than failure-path testing per se. Each of those would otherwise be a fabricated citation sitting in a wiki whose whole premise is traceability. Cost: the writer reads notes it was going to need anyway. The report-back line makes the drops auditable instead of silent.

### Suggestion 8: Use the sitemap, not the feed, to enumerate a blog — `AGENTS-MD-2b0a7d920f`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-2b0a7d920f-use-the-sitemap-not-the-feed-to-enumerate-a-blog.md](./2026-09-09-39/AGENTS-MD-2b0a7d920f-use-the-sitemap-not-the-feed-to-enumerate-a-blog.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Use the sitemap, not the feed, to enumerate a blog.** "When enumerating a site's posts for a date-bounded harvest, do not trust the RSS/Atom feed — feeds commonly carry only the latest ~10 entries. Fetch `sitemap.xml` and filter on `<lastmod>` dates; fall back to the feed only when no sitemap exists."
>
> *Grounded in: addyosmani.com's feed listing 10 posts where the sitemap yielded all 40 relevant posts since 2025-03-01.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns Phase 1 acquire step had to enumerate Addy Osmani's AI-development posts since 2025-03-01. The natural first move — fetch the RSS feed — returned exactly 10 items, all from the previous six weeks. Stopping there would have silently dropped 30 of the 40 relevant posts, including the highest-value ones (`good-spec`, `intent-debt`, `agents-md`) that ended up cited across a dozen pattern pages, and nothing in the feed says it is truncated. The sitemap fetch was one extra request and a five-line parse of `<loc>`/`<lastmod>` pairs, giving the complete dated history. The asymmetry: one HTTP request versus an invisible 75% hole in the corpus that no later phase would have flagged, because triage can't rate what acquire never fetched.

### Suggestion 9: Spot-check generated files for empty extracted fields — `AGENTS-MD-f62f33497d`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-f62f33497d-spot-check-generated-files-for-empty-extracted-fields.md](./2026-09-09-39/AGENTS-MD-f62f33497d-spot-check-generated-files-for-empty-extracted-fields.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Spot-check generated files for empty extracted fields.** "After generating a file by regex-extracting sections from other documents, grep the output for empty fields and verify the extractor against every heading variant the source documents were allowed to use. An extractor written against the common case silently drops the variants."
>
> *Grounded in: `patterns-list.md` losing its one-liner for the one page using the `**Use when (at risk of it):**` heading variant.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns build generated `skill/agent-patterns/patterns-list.md` by regex-pulling each page's `**Use when:**` line. The writer subagents had been explicitly allowed a variant for anti-patterns — `**Use when (at risk of it):**` — and the extractor's `\*\*Use when:?\*\*` pattern missed it, leaving one entry's one-liner empty. The gap was caught only because the generated output was read back before committing; nothing errored. In a generated file that agents will consume as their index into the wiki, an empty description is a quiet routing failure. The check costs one grep for empty-looking rows plus comparing the regex against the variants you yourself authorized; the alternative is shipping generated artifacts whose gaps surface only when a future consumer misbehaves.

### Suggestion 10: Log platform-mandated deviations from a spec when they happen — `AGENTS-MD-0b87abcf97`

- **Per-rule file**: [./2026-09-09-39/AGENTS-MD-0b87abcf97-log-platform-mandated-deviations-from-a-spec-when-they-happen.md](./2026-09-09-39/AGENTS-MD-0b87abcf97-log-platform-mandated-deviations-from-a-spec-when-they-happen.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Log platform-mandated deviations from a spec when they happen.** "When session or platform rules override something a project spec mandates — branch naming, tooling, file placement — follow the platform rule and record the deviation and its reason in the project's log in the same working step, not at the end of the task."
>
> *Grounded in: the session-mandated `claude/…` branch replacing SPEC's `feature/agent-patterns-mvp`, logged in `log.md` at Phase 0.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The agent-patterns SPEC hard-coded `feature/agent-patterns-mvp` as the working branch; the remote session mandated `claude/agent-patterns-spec-th583c` and forbade pushing anywhere else. The platform rule wins — but a future reader of the SPEC will look for a branch that never existed, and a future agent might "fix" the discrepancy by creating it. One log entry written during Phase 0 ("branch deviation: session policy mandates…, same role") closed that trap for the cost of three lines. The same pattern covered renaming `agent-patterns-SPEC.md` to the spec's own `SPEC.md`. Deviations recorded at the moment they're made are accurate and cheap; deviations reconstructed at session end are guesses, and unrecorded ones become the next session's confusion.

## Part 4 — proposed ADRs

Architectural decisions made in this session that the user may want to record as ADRs. Each candidate has a **full draft** in the sibling directory, carrying a frozen `ADR-<hash>` ID. The user can adopt any draft by invoking the `adr` skill, which moves it to `docs/adr/NNNN-kebab-title.md` while preserving the ID.

| ID | Title | Draft |
|----|-------|-------|
| ADR-3ba016a5fe | Consolidated bibliography ids for multi-page sources | [./2026-09-09-39/ADR-3ba016a5fe-consolidated-bibliography-ids-for-multi-page-sources.md](./2026-09-09-39/ADR-3ba016a5fe-consolidated-bibliography-ids-for-multi-page-sources.md) |
| ADR-ce4eb52bb1 | Two-source threshold for adopted patterns | [./2026-09-09-39/ADR-ce4eb52bb1-two-source-threshold-for-adopted-patterns.md](./2026-09-09-39/ADR-ce4eb52bb1-two-source-threshold-for-adopted-patterns.md) |

One-line rationale per draft appears in each draft's `## Context` opening sentence; no need to duplicate it here.
